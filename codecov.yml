coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: 70%
ignore:
  - "platform/**"
  - "tools/**"
  - "tests/**"
comment:
  layout: "reach,diff,flags,files"
  behavior: default
  require_changes: true
